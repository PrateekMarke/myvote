import 'package:flutter/material.dart';

class CandidateRegisterPage extends StatelessWidget {
  const CandidateRegisterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Candidate Registration")),
      body: Center(child: Text("Candidate Registration Form (Coming Soon)")),
    );
  }
}
